#!/bin/sh
# Run the API on the host with this repo's .env (see scripts/import-agent-bob-env).
set -eu
cd "$(dirname "$0")/.."
[ -f .env ] || { echo "no .env — run scripts/import-agent-bob-env first" >&2; exit 1; }
set -a; . ./.env; set +a
cd api && exec go run ./cmd/bob
